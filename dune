(rule (target plugin_lib_uuidm.ml) (action (run touch %{target})))

(alias
  (name plugin)
  (deps plugin_lib_uuidm.cmxs)
)

(executable
  (name plugin_lib_uuidm)
  (embed_in_plugin_libraries uuidm)
  (flags -linkall)
  (modes (native plugin))
)
